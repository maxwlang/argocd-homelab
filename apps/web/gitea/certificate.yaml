apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitea-cert
spec:
  secretName: gitea-cert
  dnsNames: 
    - overwritten-by-secret
  issuerRef:
    kind: Issuer
    name: letsencrypt-prod